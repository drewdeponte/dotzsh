#!/usr/bin/env bash
set -e

version_string=$(/usr/local/bin/rbenv version-name)

[ -f "$(pwd)/.rbenv-gemsets" ] && gemset_string=" ⟡$(rbenv gemset active | cut -d' ' -f1)"
if [ ! $version_string = '' ]; then
  echo "$version_string$gemset_string"
fi
